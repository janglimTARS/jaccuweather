{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "A fully featured weather website using Open-Meteo API",
  "main": "src/index.js",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && wrangler dev",
    "dev:test": "npm run build && wrangler dev --env test",
    "deploy:test": "npm run build && wrangler deploy --env test",
    "deploy": "npm run build && wrangler deploy",
    "promote": "npm run build && wrangler deploy --env test && wrangler deploy"
  },
  "keywords": [
    "weather",
    "cloudflare",
    "worker"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "sharp": "^0.34.5",
    "wrangler": "^4.54.0"
  }
}
