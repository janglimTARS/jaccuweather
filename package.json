{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "A fully featured weather website using Open-Meteo API",
  "main": "src/index.js",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "keywords": [
    "weather",
    "cloudflare",
    "worker"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "sharp": "^0.34.5",
    "terser": "^5.46.0",
    "wrangler": "^4.54.0"
  }
}
